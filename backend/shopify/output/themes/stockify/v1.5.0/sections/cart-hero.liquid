<!-- theme settings -->
{% schema %}
{
  'name': 'cart-hero",
  'settings': [
    {
      'type': 'image_picker",
      'id': 'image',
      'label': 'Background image'
    },
    {
      'type': 'text',
      'id': 'title',
      'label': 'Title'
    },
    {
      'type': 'text',
      'id': 'description',
      'label': 'Description'
    }
  ],
  'presets': []
}
{% endschema %}

<!-- section content -->
<div class="cart-hero" role="region" aria-label="{{ 'cart-hero.label' | t }}">
  <img src="{{ settings.image | img_url: 'large' }}" alt="{{ settings.title | escape }}">
  <h2>{{ settings.title | escape }}</h2>
  <p>{{ settings.description | escape }}</p>
</div>

<!-- styles -->
<style>
  .cart-hero {
    background-color: {{ theme.settings.backgroundcolor | escape }};
    color: {{ theme.settings.textcolor | escape }};
  }
  .cart-hero img {
    width: 100%;
    height: auto;
  }
  .cart-hero h2 {
    font-family: 'Montserrat', sans-serif;
  }
  .cart-hero p {
    font-family: 'Lato', sans-serif;
  }
</style>