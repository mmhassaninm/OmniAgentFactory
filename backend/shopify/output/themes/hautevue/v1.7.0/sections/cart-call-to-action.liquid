{% schema %}
{
  'name': 'cart-call-to-action'
}
{
  'settings': [
    {
      'type': 'text',
      'id': 'label',
      'label': 'Button label',
      'default': 'Checkout'
    },
    {
      'type': 'url',
      'id': 'url',
      'label': 'Button URL'
    }
  ]
}
{
  'presets': [
    {
      'name': 'default',
      'settings': {
        'label': 'Checkout',
        'url': '/checkout'
      }
    }
  ]
}
<button class="cart-call-to-action" aria-label="{{ settings.label | escape }}">
  <a href="{{ settings.url | escape }}">{{ settings.label | escape }}</a>
</button>
{% schema %}{% endschema %}