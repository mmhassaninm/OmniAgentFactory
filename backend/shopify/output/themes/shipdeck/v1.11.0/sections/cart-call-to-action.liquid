<div class="cart-call-to-action">
  <h2>{{ settings.heading }}</h2>
  <p>{{ settings.subheading }}</p>
  <button>{{ settings.cta_label }}</button>
</div>
{% schema %}
{
  "name": "cart-call-to-action",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Proceed to Checkout"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Complete your order now"
    },
    {
      "type": "text",
      "id": "cta_label",
      "label": "Button label",
      "default": "Checkout"
    },
    {
      "type": "url",
      "id": "cta_url",
      "label": "Button URL"
    }
  ],
  "presets": [
    {
      "name": "Default"
    }
  ]
}
{% endschema %}