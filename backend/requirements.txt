# ── Core Framework ──────────────────────
fastapi
uvicorn[standard]
pydantic
python-dotenv

# ── Database ────────────────────────────
motor
pymongo
chromadb
numpy<2.0

# ── AI / LLM ───────────────────────────
litellm
langgraph

# ── Scheduling ──────────────────────────
apscheduler

# ── HTTP & Web ──────────────────────────
httpx
requests
beautifulsoup4
duckduckgo-search

# ── Security ───────────────────────────
cryptography

# ── Utilities ──────────────────────────
psutil
docker
sentence-transformers
websockets
pystray>=0.19.0
pillow>=10.0.0

# ── Shopify Theme Factory ───────────────
semver==3.0.2                       # semantic versioning for theme versions
lxml==5.2.2                         # fast HTML/XML parser for market scraper
aiofiles==23.2.1                    # async file I/O for theme builder

# ── Money Agent ─────────────────────────
playwright                      # browser automation (already used in browser_tool.py)
anthropic                       # Claude API for pitch generation
python-telegram-bot>=20.0       # richer Telegram bot utilities (optional, httpx used directly)
google-api-python-client        # Gmail / Google APIs
google-auth-httplib2
google-auth-oauthlib

# ── Desktop Control (Windows Host Only - excluded from Docker) ──────
# pyautogui>=0.9.54               # mouse/keyboard automation (Windows only)
# pynput>=1.7.6                   # alternative for keyboard control (Windows only)
# pywin32>=306                    # Windows-specific utilities (Windows only)