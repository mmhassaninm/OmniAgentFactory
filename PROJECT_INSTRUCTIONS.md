⚠️ ══════════════════════════════════════════════════════════════════════
                  CRITICAL PROTECTED FILE — DO NOT DELETE
══════════════════════════════════════════════════════════════════════ ⚠️

  DELETING THIS FILE IS STRICTLY AND ABSOLUTELY FORBIDDEN UNDER ANY
  CIRCUMSTANCE, BY ANY AGENT, TOOL, SCRIPT, OR MANUAL ACTION.

  Any deletion of content, replacement of any section, or structural
  modification to this file MUST NOT be executed unless the agent has
  verified with 100% certainty that:
    (1) the change is logically sound and strictly necessary,
    (2) the full impact on the rest of the system is understood,
    (3) no historical record or permanent standard is being erased.

  When in doubt — DO NOT EDIT. Abort and report instead.

⚠️ ══════════════════════════════════════════════════════════════════════

# PROJECT_INSTRUCTIONS

## Primary Objective (الهدف الأساسي للمشروع)
- **Core Mission**: تحقيق دخل مادي سلبي مستدام وتوليد قيمة مالية وفائدة حقيقية للمالك بدون أي تدخل بشري مباشر (Achieving sustainable passive financial income and generating real economic value for the owner with zero direct human intervention). All development, breeding, culling, and evolution of agents must align with and optimize for this primary financial objective.

## Project Overview
- **Project Name**: OmniBot / NexusOS — Autonomous Agent Factory
- **Description**: An autonomous factory for generating, breeding, culling, and evolving AI agents. Equipped with a self-evolution engine that continuously improves the codebase itself to maximize the efficiency of its passive income streams and financial yields.

## Technology Stack
- **Backend**: Python 3.11, FastAPI, LangGraph, MongoDB, ChromaDB, LiteLLM
- **Frontend**: React, Vite, TypeScript, Tailwind CSS
- **Orchestration**: Docker Compose, PowerShell (local)
- **Database**: MongoDB running on port 27017, ChromaDB on its dedicated service port

## Development Standards
- All backend code must run within Docker Compose (Linux target, forward-slash paths).
- No Windows paths `C:\` inside Dockerfiles or compose configurations.
- Use PowerShell ONLY for local shell commands.
- Never edit files without first viewing them.
- Append all file modifications to `MODIFICATION_HISTORY.md` using the exact markdown format.

## 🚫 ABSOLUTE PROHIBITION OF UNCHECKED FILE DELETIONS (الحظر التام لحذف الملفات دون دراسة شاملة)

> [!CAUTION]
> **STRICT FILE DELETION RESTRICTION / قاعدة صارمة تمنع الحذف العشوائي للملفات**
>
> ⚠️ **ENGLISH**: UNDER NO CIRCUMSTANCES shall any file, script, component, asset, or documentation in this project be deleted, archived, or permanently removed by any agent, tool, or script without performing a **COMPREHENSIVE CODEBASE-WIDE IMPACT ASSESSMENT**. You must meticulously analyze all references, imports, dependencies, data-flows, and database bindings associated with that file. Deletion is strictly prohibited unless it is mathematically and logically proven with 100% certainty that removing the file will cause zero functional degradation, regressions, or system-wide errors.
>
> ⚠️ **العربية**: **يُمنع منعاً باتاً وبشكل قاطع** حذف، أو أرشفة، أو إزالة أي ملف، أو سكربت، أو مكون (component)، أو أصل مرئي، أو ملف توثيقي في هذا المشروع من قِبل أي عميل ذكاء اصطناعي (Agent)، أو أداة، أو سكربت برمجي دون إجراء **دراسة وتقييم شاملين ومفصلين لتأثير هذا الحذف على المشروع ككل**. يجب عليك فحص كافة الاستدعاءات (imports)، والاعتماديات (dependencies)، وتدفق البيانات، والروابط البرمجية بقاعدة البيانات للتأكد واليقين التام بنسبة 100% أن عملية الحذف لن تتسبب في أي تراجع برميجي، أو توقف للخدمات، أو ضرر بهيكل المشروع ككل.

## 🌐 DEFAULT LANGUAGE STANDARD (معيار اللغة الافتراضية للمشروع)

> [!IMPORTANT]
> **ENGLISH AS THE PRIMARY PROJECT LANGUAGE / اللغة الإنجليزية هي اللغة الأساسية والافتراضية**
>
> ⚠️ **ENGLISH**: The absolute default and primary language for the entire user interface, code symbols, endpoints, and documentation of this project is **ENGLISH**. All interface elements, pages, tools, and logs must remain entirely in English. If an Arabic version is requested or created in the future, it must be maintained as a **SEPARATE, DEDICATED ARABIC BUILD OR BRANCH**, and Arabic translations should be added **ONLY** within that specific Arabic version. The main codebase must never be diluted or mixed with multiple default languages.
>
> ⚠️ **العربية**: إن اللغة الافتراضية والأساسية المطلقة لكافة عناصر واجهة المستخدم، الرموز البرمجية، المسارات، والتوثيقات في هذا المشروع هي **اللغة الإنجليزية**. يجب أن تظل جميع المكونات والصفحات والأدوات باللغة الإنجليزية تماماً. في حال طُلب أو تم إنشاء نسخة عربية للمشروع مستقبلاً، يجب أن يتم ذلك في **نسخة أو فرع مستقل ومخصص بالكامل للغة العربية**، ويتم عمل الترجمة العربية **فقط** داخل تلك النسخة المخصصة، مع الحفاظ على كود المشروع الرئيسي خالياً تماماً من خلط اللغات أو الترجمات المشتركة.

## 🚨 MANDATORY POST-MODIFICATION BROWSER VERIFICATION (التحقق الصارم والإلزامي بعد كل تعديل)

> [!CAUTION]
> **ABSOLUTE ZERO-TOLERANCE COMPLIANCE RULE / قاعدة صارمة لا تقبل التهاون مطلقا**
>
> ⚠️ **ENGLISH**: ANY modification, edit, addition, translation, or repair performed on the web interface (Frontend) or backend routing layer **MUST** be verified immediately via a live, real browser session using the `browser_subagent` tool. You must visually inspect every component, state-change, form field, and interactive click-flow to ensure 100% functional correctness, visual excellence, and zero React-level or Console-level errors (such as hook order variations). Never trust code compilation or static build checks alone. Doing so is an automatic failure of the agent governance protocol.
>
> ⚠️ **العربية**: **يُحظر منعاً باتاً** وتحت أي ظرف من الظروف إجراء أي تعديل، أو إضافة، أو ترجمة، أو إصلاح على الواجهة البرمجية (Frontend) أو طبقة التوجيه البرمجية (Backend routing) دون القيام فوراً وبشكل إلزامي بالتحقق الفعلي الحي باستخدام أداة `browser_subagent` (المساعد المتصفح). يجب عليك فحص وتجربة كل عنصر مرئي، وكل تغيير في الحالة، وكل حقل إدخال، والتحقق من تدفق النقرات لضمان خلو النظام تماماً من أي أخطاء (مثل أخطاء ترتيب الـ Hooks) ولضمان مظهر بريميوم راقي جداً. لا تثق أبداً بمجرد نجاح بناء الكود (build compilation) دون تجربة فعلية حية في المتصفح!

### Verification Checklist (قائمة التحقق الإلزامية):
1. **Live Routing Check**: Navigate to the exact URL of the modified view. Confirm it loads cleanly and displays as intended (zero endless loading spinners).
2. **Interactive States & Binding**: Click on, type into, and interact with all modified form inputs, buttons, and switches.
3. **Database & API Integration**: Submit forms or perform actions to verify that frontend requests map correctly to the backend port (`3001` or proxy) and write successfully to MongoDB.
4. **Zero Console Warning/Error Rule**: Inspect the browser logs. There must be zero React crashes, hook misalignments, or network 404/500 errors.
5. **Visual Handoff Proof**: Capture full-screen screenshots of the verified states and include them in the final walkthrough report.

